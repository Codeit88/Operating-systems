#include<stdio.h>
#include<stdlib.h>
#include<string.h>
#include<sys/stat.h>
int main(int argc,char*argv[]){
int size=strlen(argv[1]);
char comm[size];
char name[15];
char mk_dir[]="mkdir";
int game;
strcpy(comm,argv[1]);
if(strcmp(mk_dir,comm)==0)
game=mkdir("g1", 0735);
printf("naa");
return 0;
}
